https://github.com/TheRealGudPerson/schedule-tracker/releases/latest

^^^ First working release of the code! I intend to rewrite this project in C# and Avalonia (currently undecided) in an effort to make the app's UI and performance better.

## Edit:
I modified the original python file to work properly/as intended. This means that the above builds are outdated but I cannot compile prebuilt apps to replace them at the moment. If you wish, feel free to download the python file and compile it with the pyinstaller module! run ```pip install pyinstaller``` then ```pyinstaller --onefile --windowed schedule_planner.py```.
